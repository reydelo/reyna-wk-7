<table class="table table-striped">
  <thead>
    <tr>
      <th>User Index</th>
      <!-- <th colspan="3"></th> -->
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.user_name, user_path(user) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
